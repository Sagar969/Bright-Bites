
const Reservation = () => {
  return (
    <section className='reservation section-wrapper'>
        <h2 className="section-heading cs cs-text">Reservation</h2>
        <article></article>
        <button>Book a Seat</button>
    </section>
  )
}

export default Reservation